#if !defined(HAMMING_H)
#define HAMMING_H
#include <string>
namespace hamming {
    unsigned int compute(const std::string& left, const std::string& right);
}  // namespace hamming

#endif // HAMMING_H